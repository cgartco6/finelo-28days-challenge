{
  "name": "finelo-28day-challenge",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "tsx": "^4.20.3",
    "@types/node": "^22.15.17"
  }
}
